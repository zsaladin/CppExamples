#pragma once
#include <vector>
#include <string>

namespace Consts
{
    const std::vector<std::string> BOARD_ITEM_PATHS = {
        R"(PNG\3.png)",
        R"(PNG\5.png)",
        R"(PNG\6.png)",
        R"(PNG\7.png)",
        R"(PNG\11.png)",
        R"(PNG\13.png)",
        R"(PNG\14.png)",
        R"(PNG\15.png)",
    };

    const int BOARD_SIZE = 12;
    const int BOARD_ITEM_SIZE = 60;
    const int MATCH_SIZE = 3;
}
